lute value of an integer
 * @n: the int to check
 #include "main.h"

/**
 * _abs - computes the abso* Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
